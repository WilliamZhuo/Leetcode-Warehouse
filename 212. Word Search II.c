//Recoding FSM -> 
// bfs with distance(correct state -> zero distance, wrong state->infinite distance) 

class Solution {
public:
    vector<string> findWords(vector<vector<char>>& board, vector<string>& words) {
        
    }
};